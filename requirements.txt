# Phase 0 and the deterministic pipeline use Python's standard library.
# These SDKs are imported lazily by the diagnosis providers so the app can
# still run its hardcoded escalation fallback when keys are not configured.
google-generativeai
groq
pytest